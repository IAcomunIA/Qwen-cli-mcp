{
  "name": "github-mcp",
  "version": "1.0.0",
  "description": "Servidor MCP para conectar Qwen Code CLI con la API de GitHub.",
  "scripts": {
    "start": "dotenv -- npx -y @modelcontextprotocol/server-github",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "qwen-code-cli",
    "mcp",
    "github",
    "anime-platform"
  ],
  "author": "kasnino",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/server-github": "latest",
    "dotenv-cli": "^7.4.2"
  }
}